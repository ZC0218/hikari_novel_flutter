name: Build Unsigned IPA (Flutter 3.38.7)
run-name: 编译无签名IPA | Flutter 3.38.7 Stable
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # 支持网页手动触发构建

jobs:
  build_ios_unsigned:
    runs-on: macos-latest
    steps:
      # 1. 拉取仓库全部源码
      - name: 检出仓库代码
        uses: actions/checkout@v4

      # 2. 配置对应版本Flutter（匹配作者本地3.38.7 stable）
      - name: 安装 Flutter 3.38.7 Stable
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.7'
          channel: stable
          cache: true

      # 3. 校验完整Flutter环境（移除带-y的错误许可步骤）
      - name: Flutter环境校验
        run: flutter doctor -v

      # 4. 清理缓存 + 拉取项目依赖（自动生成ios/Podfile，解决之前Podfile缺失报错）
      - name: 清理项目缓存
        run: flutter clean

      - name: 拉取pub依赖，生成iOS工程文件
        run: flutter pub get

      # 5. 安装CocoaPods（Podfile已由pub get生成）
      - name: 安装iOS Pod依赖
        run: |
          cd ios
          pod repo update
          pod install --verbose

      # 6. 编译Release无签名IPA，仅arm64架构适配新版macOS runner
      - name: 构建未签名Release IPA
        run: flutter build ipa --release --no-codesign --target-platform=ios-arm64

      # 7. 打印构建产物目录
      - name: 查看生成IPA文件
        run: ls -lh build/ios/ipa/

      # 8. 上传IPA到工作流制品
      - name: 上传未签名IPA文件
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-hikari-novel-ipa-flutter3.38.7
          path: build/ios/ipa/*.ipa
          retention-days: 30
